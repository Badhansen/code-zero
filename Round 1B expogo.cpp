#include <bits/stdc++.h>
using namespace std;
using Long = long long int;

bool rec(Long a, Long b, int mv){
    if
}

int main(){
    int t, ts = 1;
    scanf("%d", &t);
    while(t--){
        scanf("%lld%lld", &x, &y);

        bool flag = true;
        int cnt = 1e3;

        if(ans == ""){
            printf("Case #%d: IMPOSSIBLE\n", ts++);
        }
        else{
            printf("Case #%d: %s\n", ts++, ans.c_str());
        }
    }
    return 0;
}

